# OpenAlphaOperator — core dependencies (pinned ranges for reproducible installs)

# API / reactor
fastapi>=0.115.0,<1.0
uvicorn[standard]>=0.32.0,<1.0
pydantic>=2.9.0,<3.0
httpx>=0.27.0,<1.0
python-dotenv>=1.0.0,<2.0

# Cache / data
redis>=5.0.0,<6.0
pandas>=2.2.0,<3.0
numpy>=1.26.0,<3.0

# Dashboard
streamlit>=1.40.0,<2.0
plotly>=5.24.0,<6.0

# Snowflake + orchestration
snowflake-connector-python>=3.12.0,<4.0
temporalio>=1.7.0,<2.0

# Logging
structlog>=24.0.0,<26.0

# Optional extras (uncomment as needed)
# prometheus-client>=0.21.0,<1.0
# pytest>=8.0.0,<9.0
# ruff>=0.8.0,<1.0
